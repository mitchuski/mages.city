CONFORM ADVISORY (1):
  ~ Φ_inference ≈ 0 (D4b): proposer and prover are the same model ((caller default)). By the PVM the separation term is zero — the prover shares the proposer's blind spots. This is legible, not fatal; a cross-model prover is a pre-registered prediction (CR-H10). Set config.seatOpts.assay.model to a different model to raise Φ_inference.
CONFORM FAIL (1):
  - frontier.json unreadable or malformed: function is not iterable (cannot read property Symbol(Symbol.iterator))
